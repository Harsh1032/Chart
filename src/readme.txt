Inside src app is the inbuilt class which I've used as the parent class which contains the data for the 
chart.
Then inside the app folder I've created a test folder that contains the child class and extracting information
from the parent class.